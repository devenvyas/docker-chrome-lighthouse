# Lighhouse in Docker with Chrome
- Run lighthouse performance tests in a docker container
- Build with - `docker build -t light-chrome .`
- Run with - `docker run --rm -t -e URL=https://example.com light-chrome`
